name: sorting_spa
description: A single page application visualizing various sorting algorithms.

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  animation_loop:
    git: https://github.com/WissenIstNacht/animation_loop.git

dev_dependencies:
  build_runner: ^1.10.0
  build_web_compilers: ^2.11.0
  pedantic: ^1.9.0
